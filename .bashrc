# ~/.bashrc - Bash configuration
# Source this file from ~/.bash_profile or ~/.bashrc

# Claude Agent Layer
source ~/.claude/claude-agent-layer/aliases.sh

# Add custom bin to PATH
export PATH="$PATH:$HOME/bin"

# Go path
export PATH="$PATH:$HOME/go/bin"

# Editor
export EDITOR="code"
